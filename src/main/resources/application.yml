spring:
  datasource:
    url: jdbc:postgresql://dpg-d35eh8buibrs73d3ffk0-a.virginia-postgres.render.com:5432/kronos_db_prod_h0u9
    username: kronos
    password: 4VvJDEt1qj6DYwOBtRcNpZATQQijHPMA
    driver-class-name: org.postgresql.Driver
  redis:
    host: localhost
    port: 6379
  mail:
    host: smtp.gmail.com
    port: 587
    username: kronos.time.tech.solutions@gmail.com
    password: zudw foob eprd oacn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  cloud:
    gcp:
      project-id: kronos-tech-solutions
      pubsub:
        subscriber:
          subscriptions:
            time-record-approval-subscription: # Nome da subscrição no Listener
              topic: ${GCP_PUBSUB_TOPIC_NAME:time-record-approval-topic}
              auto-create: true
            password-reset-subscription:
              topic: ${PASSWORD_RESET_TOPIC:password-reset-topic}
              auto-create: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
  metrics:
    export:
      prometheus:
        enabled: true
logging:
  pattern:
    console: "${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}"
  level:
    org.springframework:
      security: TRACE

jwt:
  secret: "***JWT_SECRET_REMOVED***"
  expiration: 3600000

gcp:
  storage:
    bucket-name: kronos-tech-solutions-bucket

frontend:
      # Se estiver rodando localmente (Vite), use localhost:5174
      # Em produção (Render/Vercel), substitua pela URL real (ex: https://kts-user-plataform.onrender.com)
  base-url: ${FRONTEND_BASE_URL:http://localhost:5174}