# backend-contract-guard

Valide o back-end na branch `new-ui` para os Épicos 0 e 1 do front-end.

Leia `docs/new-ui/epic-00-01/backend-contract-guard.md`.

Execute quando viável:

```bash
./gradlew test
./gradlew build
```

Registre o resultado em `docs/new-ui/epic-00-01/backend-contract-validation.md`.
