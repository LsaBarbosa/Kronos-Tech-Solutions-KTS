{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 1,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        { "expr": "increase(kronos_auth_login_success_total[15m]) or on() vector(0)", "legendFormat": "login senha sucesso" },
        { "expr": "increase(kronos_auth_login_failure_total[15m]) or on() vector(0)", "legendFormat": "login senha falha" },
        { "expr": "increase(kronos_auth_face_login_success_total[15m]) or on() vector(0)", "legendFormat": "login facial sucesso" },
        { "expr": "increase(kronos_auth_face_login_failure_total[15m]) or on() vector(0)", "legendFormat": "login facial falha" }
      ],
      "title": "Autenticacao",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        { "expr": "increase(kronos_time_record_checkin_success_total[15m]) or on() vector(0)", "legendFormat": "check-in" },
        { "expr": "increase(kronos_time_record_checkout_success_total[15m]) or on() vector(0)", "legendFormat": "check-out" },
        { "expr": "increase(kronos_time_record_failure_total[15m]) or on() vector(0)", "legendFormat": "falhas" }
      ],
      "title": "Registro de ponto",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        { "expr": "increase(kronos_document_upload_success_total[15m]) or on() vector(0)", "legendFormat": "upload" },
        { "expr": "increase(kronos_document_download_success_total[15m]) or on() vector(0)", "legendFormat": "download" },
        { "expr": "increase(kronos_document_delete_success_total[15m]) or on() vector(0)", "legendFormat": "delete" }
      ],
      "title": "Documentos",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "targets": [
        { "expr": "increase(kronos_legal_afd_generation_success_total[15m]) or on() vector(0)", "legendFormat": "AFD" },
        { "expr": "increase(kronos_legal_aej_generation_success_total[15m]) or on() vector(0)", "legendFormat": "AEJ" },
        { "expr": "increase(kronos_legal_point_mirror_generation_success_total[15m]) or on() vector(0)", "legendFormat": "Espelho" },
        { "expr": "increase(kronos_legal_technical_certificate_success_total[15m]) or on() vector(0)", "legendFormat": "Atestado" }
      ],
      "title": "Legal/Fiscal",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 16 },
      "id": 5,
      "targets": [
        { "expr": "increase(kronos_scheduler_execution_success_total[30m]) or on() vector(0)", "legendFormat": "sucesso" },
        { "expr": "increase(kronos_scheduler_execution_failure_total[30m]) or on() vector(0)", "legendFormat": "falha" }
      ],
      "title": "Schedulers",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 16 },
      "id": 6,
      "targets": [
        { "expr": "kronos_ntp_drift_seconds or on() vector(0)", "legendFormat": "drift NTP" }
      ],
      "title": "Drift NTP",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 24 },
      "id": 7,
      "targets": [
        { "expr": "increase(kronos_company_created_total[15m]) or on() vector(0)", "legendFormat": "empresa criada" },
        { "expr": "increase(kronos_company_updated_total[15m]) or on() vector(0)", "legendFormat": "empresa atualizada" },
        { "expr": "increase(kronos_user_created_total[15m]) or on() vector(0)", "legendFormat": "usuário criado" },
        { "expr": "increase(kronos_user_updated_total[15m]) or on() vector(0)", "legendFormat": "usuário atualizado" }
      ],
      "title": "Empresas e Usuários",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 24 },
      "id": 8,
      "targets": [
        { "expr": "increase(kronos_employee_created_total[15m]) or on() vector(0)", "legendFormat": "funcionário criado" },
        { "expr": "increase(kronos_employee_updated_total[15m]) or on() vector(0)", "legendFormat": "funcionário atualizado" },
        { "expr": "increase(kronos_biometric_enrollment_success_total[15m]) or on() vector(0)", "legendFormat": "enrollment biométrico sucesso" },
        { "expr": "increase(kronos_biometric_enrollment_failure_total[15m]) or on() vector(0)", "legendFormat": "enrollment biométrico falha" }
      ],
      "title": "Funcionários e Biometria",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 32 },
      "id": 9,
      "targets": [
        { "expr": "increase(kronos_consent_accepted_total[15m]) or on() vector(0)", "legendFormat": "consentimento aceito" },
        { "expr": "increase(kronos_consent_revoked_total[15m]) or on() vector(0)", "legendFormat": "consentimento revogado" }
      ],
      "title": "LGPD Consentimento",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 32 },
      "id": 10,
      "targets": [
        { "expr": "increase(kronos_geolocation_lookup_success_total[15m]) or on() vector(0)", "legendFormat": "lookup sucesso" },
        { "expr": "increase(kronos_geolocation_lookup_failure_total[15m]) or on() vector(0)", "legendFormat": "lookup falha" },
        { "expr": "rate(kronos_geolocation_lookup_duration_seconds_sum[5m]) / rate(kronos_geolocation_lookup_duration_seconds_count[5m]) or on() vector(0)", "legendFormat": "latência média (s)" }
      ],
      "title": "Geolocalização",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 40 },
      "id": 11,
      "targets": [
        { "expr": "increase(kronos_time_adjustment_requested_total[15m]) or on() vector(0)", "legendFormat": "ajuste solicitado" },
        { "expr": "increase(kronos_time_adjustment_approved_total[15m]) or on() vector(0)", "legendFormat": "ajuste aprovado" },
        { "expr": "increase(kronos_time_adjustment_rejected_total[15m]) or on() vector(0)", "legendFormat": "ajuste rejeitado" },
        { "expr": "increase(kronos_vacation_requested_total[15m]) or on() vector(0)", "legendFormat": "férias solicitadas" },
        { "expr": "increase(kronos_vacation_approved_total[15m]) or on() vector(0)", "legendFormat": "férias aprovadas" },
        { "expr": "increase(kronos_vacation_rejected_total[15m]) or on() vector(0)", "legendFormat": "férias rejeitadas" },
        { "expr": "increase(kronos_time_off_requested_total[15m]) or on() vector(0)", "legendFormat": "folga solicitada" },
        { "expr": "increase(kronos_time_off_approved_total[15m]) or on() vector(0)", "legendFormat": "folga aprovada" },
        { "expr": "increase(kronos_time_off_rejected_total[15m]) or on() vector(0)", "legendFormat": "folga rejeitada" }
      ],
      "title": "Solicitações de Ponto",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "title": "KRONOS Negocio",
  "uid": "kronos-business"
}
